PROJECT_NAME=LongView
API_V1_STR=/api/v1
# SECRET_KEY : À définir comme variable d'environnement en production
# Valeur par défaut uniquement pour le développement local
# Générer une clé forte : python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=change-me-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=1440
ALGORITHM=HS256

# Configuration de la base de données
# En production sans gestion de comptes, peut être désactivé pour simplifier le déploiement
ENABLE_DATABASE=true
DATABASE_URL=postgresql+psycopg://postgres:%26Sylvain0@localhost:5432/longview

CORS_ORIGINS=http://localhost:5173
ENVIRONMENT=local
DEBUG=true

